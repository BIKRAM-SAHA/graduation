import CelebrateScene from "@/components/scenes/CelebrateScene";
import { SafeAreaView } from "react-native-safe-area-context";

export default function TabTwoScreen() {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <CelebrateScene />
        </SafeAreaView>
    );
}
